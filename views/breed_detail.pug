extends layout

block content
    div(class="d-flex col-12 flex-column justify-content-center gap-10")
        h3 #{title}
        p #{breed.desc}

    if errors
        ul
            for error in errors
                li!= error.msg

            